version: 1
taskDefinition:
  containerDefinitions:
    - name: container-service
      image: 
      cpu: 256
      memory: 512
      portMappings:
        - containerPort: 80
          hostPort: 80
      essential: true
